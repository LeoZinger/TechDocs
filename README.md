# TechDocs
Technical Documents
CREATE TABLE CUSTOMERS(
   ID   INT              NOT NULL,
   NAME VARCHAR (20)     NOT NULL,
   ReferredBy INT				,
   SALARY   DECIMAL (18, 2),       
   PRIMARY KEY (ID)
);
